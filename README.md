# MNIST-Digit-recognizer

Handwritten digit recognition using deep learning models trained on the MNIST dataset.

## 📌 Overview
Classification model for recognizing handwritten digits (0-9) using neural networks built with TensorFlow/Keras on the classic MNIST dataset.
